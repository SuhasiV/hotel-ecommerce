import {
  Cinzel,
  Philosopher,
  Cormorant,
  Kurale,
  Livvic,
} from "next/font/google";

export const cinzel = Cinzel({
  subsets: ["latin"],
  display: "swap",
});

export const philosopher = Philosopher({
  subsets: ["latin"],
  display: "swap",
});
